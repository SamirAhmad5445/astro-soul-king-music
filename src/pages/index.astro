---
const username = Astro.cookies.get("username")
const role = Astro.cookies.get("role")

if (!username || !role) {
  return Astro.redirect("/register");
}

return Astro.redirect(
  role! === "user"
  ? "/login"
  : "/artist/login"
)
---